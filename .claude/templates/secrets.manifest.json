{
  "project": "{{project}}",
  "secrets": [
    {
      "name": "EXAMPLE_SIGNING_SECRET",
      "class": "generated",
      "components": ["web"],
      "envs": ["local", "staging", "prod"],
      "description": "Replace with real entries: class generated|provided|public; components must match register_project; provided entries carry an obtain hint."
    },
    {
      "name": "EXAMPLE_API_KEY",
      "class": "provided",
      "components": ["web"],
      "envs": ["local", "staging", "prod"],
      "obtain": "provider.com → API Keys"
    },
    {
      "name": "NEXT_PUBLIC_EXAMPLE",
      "class": "public",
      "components": ["web"]
    }
  ]
}
